@model List<ShippingManagement.Web.Models.Company>
@{
    Layout = null;
    string title = ViewBag.Title ?? "Company Fleet Report";
    string? subtitle = ViewBag.Subtitle;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@title</title>
    <style>
        body { font-family:'Segoe UI',Arial,sans-serif; font-size:11px; margin:24px; color:#111; }
        .rpt-head { display:flex; align-items:center; gap:10px; border-bottom:3px solid #22a6f2; padding-bottom:8px; margin-bottom:6px; }
        .rpt-mark { width:34px; height:34px; border-radius:9px; background:linear-gradient(135deg,#22a6f2,#1ad1a5); color:#fff; display:flex; align-items:center; justify-content:center; font-size:18px; }
        h2 { margin:0; color:#0d2438; } .sub { color:#555; margin-bottom:14px; }
        table { width:100%; border-collapse:collapse; }
        th,td { border:1px solid #aab4c0; padding:3px 5px; text-align:left; }
        th { background:#0d2438; color:#fff; }
        tr:nth-child(even) td { background:#f6f9fc; }
        tr.regular td { background:#d6ecff; }
        @@media print { .noprint { display:none; } }
    </style>
</head>
<body onload="window.print()">
    <div class="noprint" style="margin-bottom:10px">
        <button onclick="window.print()">Print / Save as PDF</button>
        <button onclick="window.close()">Close</button>
    </div>
    <div class="rpt-head"><div class="rpt-mark">&#9875;</div><h2>@title</h2></div>
    <div class="sub">@subtitle &middot; @Model.Count company(ies) &middot; Generated @DateTime.Now.ToString("yyyy-MM-dd HH:mm")</div>
    <table>
        <thead><tr><th>#</th><th>Company</th><th>Address</th><th>Country</th><th>General Email</th><th>Website</th><th>Telephone</th><th>Fleet Size</th><th>Status</th></tr></thead>
        <tbody>
            @{ int i = 1; }
            @foreach (var c in Model)
            {
                <tr class="@(c.Status == "Regular" ? "regular" : "")">
                    <td>@(i++)</td><td>@c.CompanyName</td><td>@c.Address</td><td>@c.Country</td>
                    <td>@c.GeneralEmail</td><td>@c.Website</td><td>@c.Telephone</td>
                    <td>@c.FleetCount</td><td>@c.Status</td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
